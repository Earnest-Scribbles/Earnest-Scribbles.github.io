<!-- Gives a Glitch and Turbulence to the Icon -->
<svg  xmlns="http://www.w3.org/2000/svg" version="1.1" height="0" width="0">
    <defs>
    <filter id="squiggly-0">
        <feTurbulence
        type="fractalNoise"
        id="turbulence"
        baseFrequency=".1"
        numOctaves="2"
        result="turbulence"
        seed="0"
        />
        <feDisplacementMap id="displacement" in="SourceGraphic" scale="3" />
    </filter>
    <filter id="squiggly-1">
        <feTurbulence
        type="fractalNoise"
        id="turbulence"
        baseFrequency=".4"
        numOctaves="2"
        result="turbulence"
        seed="1"
        />
        <feDisplacementMap in="SourceGraphic" scale="4" />
    </filter>

    <filter id="squiggly-2">
        <feTurbulence
        type="fractalNoise"
        id="turbulence"
        baseFrequency=".1"
        numOctaves="2"
        result="turbulence"
        seed="2"
        />
        <feDisplacementMap in="SourceGraphic" scale="3" />
    </filter>
    <filter id="squiggly-3">
        <feTurbulence
        type="fractalNoise"
        id="turbulence"
        baseFrequency=".4"
        numOctaves="2"
        result="turbulence"
        seed="3"
        />
        <feDisplacementMap in="SourceGraphic" scale="4" />
    </filter>
    </defs>
</svg>